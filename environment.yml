name: Sai2608
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - numpy
  - pandas
  - pyarrow
  - poppler      # PDF rendering for the parse/ chain
  - tesseract    # OCR fallback, week 2
  - pip:
      - -e .[dev,corpus]
